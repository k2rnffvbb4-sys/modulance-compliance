Work Package Code: WP-1 Work Package Name: Identity & Access Objective: Deliver the minimum xxx.ai identity endpoints required to unblock all downstream Modulance work, provision internal test organizations, and prepare the customer org provisioning path. Expected Outcome: Three xxx.ai endpoints are live and reachable from Modulance dev environment. Internal Org A and Test Org B are provisioned with users, memberships, and AI configs. Bidirectional JWT verification is confirmed. Customer org provisioning path is ready and gated on Week 4 exit criteria.

Sprint Code: WP1-S1 Sprint Name: XXX.ai Minimum Endpoints Objective: Build and deploy the three identity endpoints that Modulance requires to implement JWT middleware and AI config resolution. Expected Outcome: POST /auth/login, GET /.well-known/jwks.json, and GET /api/orgs/:organizationId/ai-config are live, reachable from Modulance dev, and verified individually.

Task ID: T-1.1.1 Task Title: Implement POST /auth/login Description: Build the login endpoint on the xxx.ai platform. Must issue an RS256-signed JWT containing all 7 required claims: sub, organizationId, role, email, name, iat, exp. Token must be verifiable using the JWKS public key endpoint. Deliverable: POST /auth/login endpoint live; returns a valid RS256 JWT with all 7 claims on successful authentication. Estimated Effort: 1 pd Required Capability: XXX.ai Backend Engineering Dependencies: None Verification Criteria: POST with valid credentials returns HTTP 200 and a JWT. Decoded JWT contains all 7 claims. JWT signature verifies against the JWKS public key.

Task ID: T-1.1.2 Task Title: Implement GET /.well-known/jwks.json Description: Build the JWKS endpoint on the xxx.ai platform. Must return the RS256 public key in standard JWKS format. This endpoint is used by Modulance jwtAuthMiddleware to validate incoming JWTs. Deliverable: GET /.well-known/jwks.json endpoint live; returns valid JWKS JSON containing the RS256 public key. Estimated Effort: 0.5 pd Required Capability: XXX.ai Backend Engineering Dependencies: None — parallel with T-1.1.1 Verification Criteria: GET returns HTTP 200 with Content-Type application/json. Response contains keys array with at least one entry. Key kty is RSA. jwtAuthMiddleware can fetch and cache the key.

Task ID: T-1.1.3 Task Title: Implement GET /api/orgs/:organizationId/ai-config Description: Build the org AI config endpoint on the xxx.ai platform. Must return the AI provider configuration for the specified organization. Used by Modulance resolveAiConfig function. Must require a valid service token for authentication. Deliverable: GET /api/orgs/:organizationId/ai-config endpoint live; returns org-specific AI provider config. Estimated Effort: 1 pd Required Capability: XXX.ai Backend Engineering Dependencies: None — parallel with T-1.1.1 and T-1.1.2 Verification Criteria: GET with valid service token and existing org ID returns HTTP 200 with AI config payload. GET with unknown org ID returns HTTP 404. GET without service token returns HTTP 401.

Task ID: T-1.1.4 Task Title: Deploy and expose all three endpoints to Modulance dev environment Description: Deploy all three implemented endpoints to an environment whose URL is accessible from the Modulance development and CI environment. Provide the base URL to the Modulance team. Deliverable: All 3 endpoints reachable from Modulance dev environment. Base URL communicated to Modulance team. Estimated Effort: 0 pd (ops task) Required Capability: XXX.ai DevOps Dependencies: T-1.1.1, T-1.1.2, T-1.1.3 Verification Criteria: Modulance engineer can reach all 3 endpoints from dev machine. No firewall or CORS block on required routes.

Sprint Code: WP1-S2 Sprint Name: Org Provisioning — Internal Objective: Provision Internal Org A and Test Org B in xxx.ai with users, memberships, and AI configs. Confirm bidirectional JWT acceptance with live Modulance middleware. Expected Outcome: Both internal orgs are provisioned. Org A and Org B JWTs are accepted by Modulance jwtAuthMiddleware. AI configs are set and distinct. Bidirectional verification is confirmed.

Task ID: T-1.2.1 Task Title: Provision Internal Org A in xxx.ai Description: Create the Internal Org A organization record in the xxx.ai organizations table. Create an admin user record. Create the membership record linking the user to the org with role admin. Deliverable: Internal Org A exists in xxx.ai with admin user and membership. Org A ID value known and communicated to Modulance team. Estimated Effort: 0.25 pd Required Capability: XXX.ai Operations Dependencies: T-1.1.4 Verification Criteria: POST /auth/login with Org A admin credentials returns a JWT containing organizationId matching Org A ID and role admin.

Task ID: T-1.2.2 Task Title: Set AI config for Internal Org A Description: Set the AI provider configuration for Internal Org A via GET /api/orgs/:organizationId/ai-config. Config must include provider type and API key. Deliverable: GET /api/orgs/{OrgAId}/ai-config returns a valid AI config payload. Estimated Effort: 0.1 pd Required Capability: XXX.ai Operations Dependencies: T-1.2.1 Verification Criteria: Endpoint returns HTTP 200 with non-empty AI config for Org A ID.

Task ID: T-1.2.3 Task Title: Provision Test Org B in xxx.ai Description: Create the Test Org B organization record in xxx.ai. Create an admin user. Create the membership record with role admin. Also create a second user with role member for admin route guard testing. Deliverable: Test Org B exists in xxx.ai with admin user, member user, and memberships. Org B ID communicated to Modulance team. Estimated Effort: 0.25 pd Required Capability: XXX.ai Operations Dependencies: T-1.1.4 Verification Criteria: POST /auth/login with Org B admin credentials returns JWT with organizationId matching Org B ID and role admin. Login with Org B member credentials returns JWT with role member.

Task ID: T-1.2.4 Task Title: Set AI config for Test Org B Description: Set the AI provider configuration for Test Org B. Config must be distinct from Org A config so that AI config leakage can be detected in Week 4 V-AI tests. Deliverable: GET /api/orgs/{OrgBId}/ai-config returns a valid AI config payload distinct from Org A config. Estimated Effort: 0.1 pd Required Capability: XXX.ai Operations Dependencies: T-1.2.3 Verification Criteria: Endpoint returns HTTP 200 with non-empty AI config for Org B ID. Config value differs from Org A config value.

Task ID: T-1.2.5 Task Title: Bidirectional JWT verification — Org A and Org B Description: Using the live Modulance JWT middleware, verify that JWTs issued by xxx.ai for both Org A and Org B are accepted and correctly parsed. Both programs must be ready simultaneously. Deliverable: Confirmation that Org A JWT and Org B JWT are both accepted by Modulance jwtAuthMiddleware. getIdentity(res) returns correct organizationId and role for each. Estimated Effort: 0.25 pd Required Capability: XXX.ai Operations and Modulance Backend Engineering Dependencies: T-1.2.1, T-1.2.3, T-2.1.5 Verification Criteria: Authenticated request with Org A JWT returns getIdentity(res).organizationId equal to Org A ID. Authenticated request with Org B JWT returns getIdentity(res).organizationId equal to Org B ID. Both return HTTP 200 on a protected route.

Sprint Code: WP1-S3 Sprint Name: Org Provisioning — Customer Objective: Provision the real customer organization in xxx.ai after Week 4 exit criteria are confirmed. Deliver customer credentials and verify AI config resolution end-to-end. Expected Outcome: Real customer org exists in xxx.ai with admin user, AI config, and verified JWT. resolveAiConfig(customerOrgId) returns customer-specific key. Customer is ready for access grant in WP-7.

Task ID: T-1.3.1 Task Title: Provision real customer org in xxx.ai Description: Create the real customer organization record in xxx.ai. Create the customer admin user with their actual email address. Create the membership record with role admin. This task must not begin until T-6.3.8 confirms all 8 exit criteria pass. Deliverable: Real customer org exists in xxx.ai with admin user and membership. Estimated Effort: 0.25 pd Required Capability: XXX.ai Operations Dependencies: T-6.3.8 Verification Criteria: POST /auth/login with customer credentials returns HTTP 200 and a valid JWT containing customer organizationId and role admin.

Task ID: T-1.3.2 Task Title: Set AI config for real customer org Description: Set the AI provider configuration for the real customer org using the customer's own API key. Must not use the internal Org A key or any shared key. Deliverable: GET /api/orgs/{customerOrgId}/ai-config returns the customer's own AI provider config. Estimated Effort: 0.1 pd Required Capability: XXX.ai Operations Dependencies: T-1.3.1 Verification Criteria: Endpoint returns HTTP 200 with customer AI config. Config key value is the customer's own key, not the internal org key.

Task ID: T-1.3.3 Task Title: First login for real customer admin Description: Perform the first login for the customer admin user to confirm the JWT issuance flow works end-to-end with the real customer credentials. Deliverable: Customer admin JWT issued and available for Modulance verification. Estimated Effort: 0.05 pd Required Capability: XXX.ai Operations Dependencies: T-1.3.2 Verification Criteria: POST /auth/login with customer credentials returns HTTP 200. JWT contains organizationId matching customer org ID. JWT is RS256-signed and verifiable via JWKS.

Task ID: T-1.3.4 Task Title: Verify resolveAiConfig with real customer org ID Description: Using the live Modulance resolveAiConfig function, verify that calling resolveAiConfig with the customer org ID returns the customer's own AI key and not the internal org key or any fallback global key. Deliverable: Confirmed that resolveAiConfig(customerOrgId) returns customer-specific AI config. Estimated Effort: 0.1 pd Required Capability: XXX.ai Operations and Modulance Backend Engineering Dependencies: T-1.3.3, T-4.1.1 Verification Criteria: resolveAiConfig(customerOrgId) returns the customer AI key. resolveAiConfig(orgAId) returns Org A key. The two values are different.

Work Package Code: WP-2 Work Package Name: Organization Isolation — Schema and Data Objective: Add organization_id to all 20 org-scoped tables, backfill all existing rows to Internal Org A, and export a verified snapshot before any query-layer work begins. Expected Outcome: All 20 tables carry organization_id. Zero NULL rows exist. week1-post-backfill snapshot is in persistent storage. JWT middleware stack is live and unit-tested.

Sprint Code: WP2-S1 Sprint Name: JWT Middleware Stack Objective: Implement and register jwtAuthMiddleware, requireRole, and getIdentity. Apply the middleware to all /api/* routes. Confirm with a full unit test suite. Expected Outcome: All /api/* routes require a valid JWT. Role guard returns HTTP 403 on mismatch. getIdentity returns typed claims. All 11 unit tests pass.

Task ID: T-2.1.1 Task Title: Implement jwtAuthMiddleware Description: Create src/server/middleware/jwt-auth.ts. Must fetch and cache the JWKS public key from the xxx.ai JWKS endpoint with a 5-minute TTL. Must validate incoming JWTs: RS256 algorithm, signature, expiry. Must extract all 7 claims and attach them to res.locals. Must return HTTP 401 on missing or invalid token. JWKS URL must be configurable via environment variable so it can be mocked for unit tests. Deliverable: src/server/middleware/jwt-auth.ts — JWKS-validating JWT middleware with 5-min key cache. Estimated Effort: 1 pd Required Capability: Modulance Backend Engineering Dependencies: T-1.1.2 Verification Criteria: Valid JWT causes middleware to call next() and res.locals contains all 7 claims. Expired JWT returns HTTP 401. Tampered JWT returns HTTP 401. Missing Authorization header returns HTTP 401. JWKS key is cached and not re-fetched within 5 minutes.

Task ID: T-2.1.2 Task Title: Implement requireRole middleware factory Description: Create src/server/middleware/require-role.ts. Must export a factory function requireRole(role: string) that returns an Express middleware. Middleware must read the role claim from res.locals set by jwtAuthMiddleware and return HTTP 403 if the claim does not match the required role. Deliverable: src/server/middleware/require-role.ts — role guard middleware factory. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.1 Verification Criteria: Request with role admin JWT causes requireRole('admin') to call next(). Request with role member JWT causes requireRole('admin') to return HTTP 403. Missing role claim returns HTTP 403.

Task ID: T-2.1.3 Task Title: Implement getIdentity accessor Description: Create src/server/middleware/get-identity.ts. Must export a function getIdentity(res: Response) that reads the 7 JWT claims from res.locals and returns a typed identity object. Must throw a typed error if called on a response that has not been processed by jwtAuthMiddleware. Deliverable: src/server/middleware/get-identity.ts — typed identity accessor. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.1 Verification Criteria: getIdentity(res) returns correct organizationId, role, sub, email, name, iat, exp after middleware runs. Throws typed error if called without middleware having run.

Task ID: T-2.1.4 Task Title: Register jwtAuthMiddleware on all /api/* routes Description: Add app.use('/api', jwtAuthMiddleware) to src/server/entry.ts. Must be registered before all API route handlers. Confirm that all /api/* routes now require a valid JWT. Deliverable: src/server/entry.ts updated. All /api/* routes require valid JWT. Estimated Effort: 0.1 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.1, T-2.1.2, T-2.1.3 Verification Criteria: Unauthenticated request to any /api/* route returns HTTP 401. Authenticated request with valid JWT proceeds to handler.

Task ID: T-2.1.5 Task Title: Execute JWT middleware and role guard unit test suite Description: Execute the full JWT middleware and role guard unit test suite. Tests must cover: valid token accepted, expired token rejected, tampered token rejected, missing token rejected, correct claims extracted, JWKS cache behavior, role match passes, role mismatch returns 403, missing role returns 403, admin role accepted on admin route, member role rejected on admin route. Deliverable: All 11 unit tests passing. Test run output recorded. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.4 Verification Criteria: All 11 tests pass with zero failures. No skipped tests.

Sprint Code: WP2-S2 Sprint Name: Schema Migration Objective: Add organization_id column and index to all 20 org-scoped tables via idempotent migration files. Execute migrations against the preview database. Expected Outcome: All 20 tables carry organization_id column and index. Migration files are idempotent and safe to re-run after container recycle.

Task ID: T-2.2.1 Task Title: Add organizationId field to schema.ts for all 20 tables Description: Update src/server/db/schema.ts to add organizationId: varchar('organization_id', { length: 36 }) to all 20 org-scoped tables. Field must be nullable initially to allow backfill before NOT NULL constraint is applied. The 20 tables are: projects, sourceDocuments, annotations, canonicalKoAssignments, canonicalKos, digitalTwins, evidenceAnchors, pdfCoordinateAnchors, kos, productKnowledgeAcquisitions, knowledgeAcquisitions, products, requirements, interpretationRuns, interpretationConfigs, pdfStats, productKnowledgeObjects, productIntelligence, deliverableIntelligence, standardsIntelligence. Deliverable: schema.ts updated with organizationId field on all 20 tables. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: None Verification Criteria: TypeScript compilation passes. All 20 table definitions contain organizationId field. Field is nullable.

Task ID: T-2.2.2 Task Title: Write ADD COLUMN migration for all 20 tables Description: Write a migration file containing ALTER TABLE ADD COLUMN organization_id VARCHAR(36) NULL for all 20 tables. Migration must be idempotent — use ADD COLUMN IF NOT EXISTS or equivalent guard so it can be re-run after container recycle without error. Deliverable: Migration file with 20 ADD COLUMN statements. Idempotent. Estimated Effort: 0.75 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.2.1 Verification Criteria: Migration runs without error on a fresh DB. Migration re-runs without error on a DB where columns already exist.

Task ID: T-2.2.3 Task Title: Write CREATE INDEX migration for all 20 tables Description: Write a migration file containing CREATE INDEX IF NOT EXISTS idx_table_org ON table(organization_id) for all 20 tables. Must use IF NOT EXISTS to be idempotent. Deliverable: Migration file with 20 CREATE INDEX statements. Idempotent. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.2.2 Verification Criteria: Migration runs without error. SHOW INDEX FROM each table confirms index exists on all 20 tables.

Task ID: T-2.2.4 Task Title: Execute migrations against preview database Description: Execute both migration files against the preview database. Verify column and index existence on all 20 tables. Note: preview DB is ephemeral with approximately 1h40m recycle. Migration must be re-run after every container recycle until the snapshot is exported in T-2.3.4. Deliverable: Migration executed. Column and index confirmed on all 20 tables. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.2.3 Verification Criteria: SHOW COLUMNS FROM each table confirms organization_id column on all 20 tables. SHOW INDEX FROM each table confirms index on all 20 tables. No migration errors in output.

Sprint Code: WP2-S3 Sprint Name: Backfill and Snapshot Objective: Backfill all existing rows to Internal Org A ID, verify zero NULL rows, export the week1-post-backfill snapshot, and confirm snapshot import is org-aware. Expected Outcome: Zero NULL organization_id rows across all 20 tables. week1-post-backfill snapshot exists in persistent storage. Import handler stamps org ID and validates org ID match.

Task ID: T-2.3.1 Task Title: Write idempotent backfill script Description: Create src/server/db/backfill-org-id.ts. Must execute UPDATE table SET organization_id = OrgAId WHERE organization_id IS NULL for all 20 tables. Script must be idempotent — safe to re-run without duplicating or overwriting non-NULL values. Must use db.execute(sql.raw(...)) — not Drizzle .update() which silently produces affectedRows=0 with mysql2. Deliverable: src/server/db/backfill-org-id.ts — idempotent backfill script. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.2.4, T-1.2.1 Verification Criteria: Script runs without error. All 20 UPDATE statements execute. Script is safe to re-run with second run producing no changes.

Task ID: T-2.3.2 Task Title: Execute backfill script against preview database Description: Execute backfill-org-id.ts against the preview database. Deliverable: All existing rows across all 20 tables have organization_id set to Internal Org A ID. Estimated Effort: 0.1 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.3.1 Verification Criteria: Script exits with code 0. No errors in output.

Task ID: T-2.3.3 Task Title: Verify zero NULL organization_id rows across all 20 tables Description: Execute SELECT COUNT(*) FROM table WHERE organization_id IS NULL for all 20 tables. All counts must be zero before proceeding to snapshot export. Deliverable: Verification output showing COUNT = 0 for all 20 tables. Estimated Effort: 0.1 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.3.2 Verification Criteria: All 20 COUNT queries return 0. Output is recorded.

Task ID: T-2.3.4 Task Title: Export week1-post-backfill snapshot Description: Export a full database snapshot immediately after T-2.3.3 passes. Tag the snapshot week1-post-backfill. This snapshot is the safety net against ephemeral DB container recycles. Must be exported before any WP-3 work begins. Deliverable: Snapshot tagged week1-post-backfill saved to persistent storage. Estimated Effort: 0.1 pd Required Capability: Modulance Operations Dependencies: T-2.3.3 Verification Criteria: Snapshot file exists in persistent storage. Snapshot can be restored to a clean DB and produces a DB with zero NULL organization_id rows across all 20 tables.

Task ID: T-2.3.5 Task Title: Patch import-snapshot handler for org-awareness Description: Review import-snapshot/POST.ts to confirm that snapshot import stamps organization_id on all INSERT rows during import, and that the import validates that the snapshot org ID matches the requesting org ID. If either check is missing, patch the handler. Deliverable: import-snapshot/POST.ts stamps org ID on import and validates org ID match. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.3.4, T-2.1.5 Verification Criteria: Import of an Org A snapshot using an Org A JWT succeeds. Import of an Org A snapshot using an Org B JWT returns HTTP 403 or HTTP 400. All rows inserted during import carry the requesting org organization_id.

Work Package Code: WP-3 Work Package Name: Organization Isolation — Query Layer Objective: Add organization_id filters to all SELECT, UPDATE, and DELETE query sites across all handlers and services. Stamp organization_id on all INSERT sites. Replace global singletons with org-scoped Maps. Expected Outcome: Every query in the system is org-scoped. No cross-org data is accessible or mutable. All INSERT sites stamp org ID. Singletons are replaced. Zero NULL rows after test inserts.

Sprint Code: WP3-S1 Sprint Name: Group A Filters — Primary Chain Objective: Apply org filters to the highest-risk handlers in the primary data chain: projects, source documents, canonical KOs, and the line 181 promote mutation. Expected Outcome: Projects, source documents, canonical KOs, and annotation promote are all org-scoped. Line 181 raw SQL UPDATE includes org filter and has passed second-engineer review.

Task ID: T-3.1.1 Task Title: Apply org filter to projects handlers Description: Add WHERE organization_id = getIdentity(res).organizationId filter to all SELECT queries in the projects handlers. Add organization_id stamp to the POST handler INSERT. Use db.execute(sql.raw(...)) for any UPDATE/DELETE — not Drizzle .update(). Deliverable: Projects handlers return only requesting-org projects. New projects are stamped with requesting org ID. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: GET /api/projects with Org A JWT returns only Org A projects. GET /api/projects with Org B JWT returns only Org B projects. POST creates project with correct organization_id.

Task ID: T-3.1.2 Task Title: Apply org filter to all 4 query branches in source-documents/GET.ts Description: Add org filter to all 4 query branches in source-documents/GET.ts. Line 67 contains an unscoped query that must be patched. Verify all branches are covered including any conditional query paths. Deliverable: All 4 query branches in source-documents/GET.ts include org filter. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: All 4 code paths return only requesting-org source documents. Line 67 query confirmed patched. No unscoped query branch remains.

Task ID: T-3.1.3 Task Title: Apply org filter to all 5 source-document sub-resource handlers Description: Add org filter to all 5 source-document sub-resource handlers: [id]/GET.ts, DELETE.ts, archive/PUT.ts, impact/GET.ts, file/GET.ts. Each handler must verify the requested document belongs to the requesting org before returning data or performing mutations. Use db.execute(sql.raw(...)) for DELETE and PUT. These handlers are in a bracket-path directory — must use #db/client.js and #db/schema.js import aliases due to the Vite SSR bracket-path bug. Deliverable: All 5 source-document sub-resource handlers include org filter. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-3.1.2 Verification Criteria: Request for Org A document using Org B JWT returns HTTP 404 or HTTP 403. DELETE/PUT on Org A document using Org B JWT returns HTTP 404 or HTTP 403. Import aliases #db/client.js and #db/schema.js used in all bracket-path files.

Task ID: T-3.1.4 Task Title: Apply org filter to canonical KO registry and compare-merge handlers Description: Add org filter to the canonical KO registry query. Add org filter and org stamp to the compare-merge POST handler. Deliverable: Canonical KO registry and compare-merge handlers are org-scoped. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: GET /api/canonical-ko/registry with Org A JWT returns only Org A canonical KOs. POST compare-merge creates canonical KO stamped with requesting org ID.

Task ID: T-3.1.5 Task Title: Patch line 181 raw SQL UPDATE in annotations promote handler Description: Add AND organization_id = '${identity.organizationId}' to the raw SQL UPDATE at line 181 of annotations/[id]/promote/POST.ts. This is the highest-risk single line in the codebase. Without this filter, Org A can mutate Org B annotations. Must use db.execute(sql.raw(...)) — this is already a raw SQL call; the org filter must be appended to the WHERE clause. This handler is in a bracket-path directory — use #db/client.js and #db/schema.js import aliases. Deliverable: Line 181 raw SQL UPDATE includes AND organization_id = '${identity.organizationId}'. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: POST promote with Org A JWT on an Org B annotation returns HTTP 404 or HTTP 403. POST promote with Org A JWT on an Org A annotation succeeds. Raw SQL WHERE clause confirmed to include org filter.

Task ID: T-3.1.6 Task Title: Second-engineer code review of line 181 patch Description: A second engineer must review the T-3.1.5 patch before it is merged to main. This is a formal code review gate, not optional. Reviewer must confirm: org filter is present in the WHERE clause, the filter uses identity.organizationId from getIdentity(res), and no other raw SQL UPDATE in the same file is missing an org filter. Deliverable: Code review approval recorded. Patch merged to main. Estimated Effort: 0 pd (review gate) Required Capability: Modulance Backend Engineering (second engineer) Dependencies: T-3.1.5 Verification Criteria: Pull request or code review record shows approval from a second engineer. Patch is merged only after approval.

Sprint Code: WP3-S2 Sprint Name: Group B Filters — Authoring Surface Objective: Apply org filters to all annotation, digital-twin, evidence-anchor, and PDF coordinate anchor handlers. Expected Outcome: All authoring surface handlers are org-scoped. Cross-org reads and mutations on annotations, digital twins, evidence anchors, and PDF coordinate anchors are blocked.

Task ID: T-3.2.1 Task Title: Apply org filter to all 7 annotation handlers Description: Add org filter to all 7 annotation handlers: GET, overlay/GET, [id]/GET, [id]/PUT, [id]/DELETE, [id]/review/PUT, POST. GET and overlay/GET must filter by org. The [id] handlers must verify the annotation belongs to the requesting org. POST must stamp org ID. Bracket-path handlers must use #db/client.js and #db/schema.js aliases. Use db.execute(sql.raw(...)) for all UPDATE/DELETE. Deliverable: All 7 annotation handlers are org-scoped. Estimated Effort: 1 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: GET /api/annotations with Org B JWT returns only Org B annotations. PUT/DELETE on Org A annotation using Org B JWT returns HTTP 404 or HTTP 403. POST creates annotation with correct org ID.

Task ID: T-3.2.2 Task Title: Apply org filter to all 5 digital-twin handlers Description: Add org filter to all 5 digital-twin handlers: list/GET, traceability/GET, by-source/[sourceDocId]/GET, [twinId]/GET, [twinId]/reprocess/POST. Bracket-path handlers must use #db/client.js and #db/schema.js import aliases. Deliverable: All 5 digital-twin handlers are org-scoped. Estimated Effort: 0.75 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: GET /api/digital-twin/list with Org B JWT returns only Org B digital twins. Bracket-path handlers confirmed to use #db/client.js and #db/schema.js aliases.

Task ID: T-3.2.3 Task Title: Apply org filter to evidence-anchor handlers Description: Add org filter to evidence-anchors GET. Add org filter and org stamp to generate-gpt POST. Deliverable: Evidence-anchor handlers are org-scoped. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: GET /api/evidence-anchors with Org B JWT returns only Org B evidence anchors.

Task ID: T-3.2.4 Task Title: Apply org filter to PDF coordinate anchor handlers Description: Add org filter to PDF coordinate anchor GET. Add org stamp to POST. Deliverable: PDF coordinate anchor handlers are org-scoped. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: GET /api/pdf-coordinate-anchors with Org B JWT returns only Org B anchors. POST creates anchor with correct org ID.

Sprint Code: WP3-S3 Sprint Name: Group C/D Filters — KO Engine and Product Pipeline Objective: Apply org filters to KPI queries, platform snapshot, KO service, product knowledge acquisition service, and knowledge acquisition service. Expected Outcome: All KO engine and product pipeline queries are org-scoped. Full-table scans at lines 312–313 are patched. Drizzle inArray single-element array bug is mitigated with eq() loops.

Task ID: T-3.3.1 Task Title: Apply org filter to all 12 KPI query sites Description: Add org filter to all 11 query sites in kpis/service.ts and to kpis/GET.ts. Each query must include WHERE organization_id = organizationId or equivalent Drizzle filter. Deliverable: All 12 KPI query sites are org-scoped. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: KPI queries for Org A return only Org A data. KPI queries for Org B return only Org B data.

Task ID: T-3.3.2 Task Title: Apply org filter to platform-snapshot handler Description: Add org filter to the platform snapshot GET handler. Deliverable: platform-snapshot/GET.ts returns only requesting-org data. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: GET /api/platform-snapshot with Org B JWT returns only Org B snapshot data.

Task ID: T-3.3.3 Task Title: Apply org filter to KO service and all KO handlers Description: Add org filter to all query sites in ko/service.ts and all KO API handlers. Note: Drizzle inArray silently fails for single-element arrays with mysql2 — use per-ID eq() loop for any array-based KO queries. Deliverable: KO service and all KO handlers are org-scoped. Estimated Effort: 0.75 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: KO queries return only requesting-org KOs. Single-element array queries confirmed to use eq() loop.

Task ID: T-3.3.4 Task Title: Patch full-table scans at lines 312–313 of product-knowledge-acquisition/service.ts Description: Add org filter to the full-table scans at lines 312–313 of product-knowledge-acquisition/service.ts. These are unfiltered queries that return data for all organizations. Deliverable: Lines 312–313 queries include org filter. No full-table scans remain. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Queries at lines 312–313 confirmed to include org filter. Query execution plan shows no full-table scan.

Task ID: T-3.3.5 Task Title: Apply org filter to knowledge-acquisition/service.ts Description: Add org filter to all query sites in knowledge-acquisition/service.ts. Deliverable: knowledge-acquisition/service.ts is org-scoped. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Knowledge acquisition queries return only requesting-org data.

Sprint Code: WP3-S4 Sprint Name: Group E/F Filters — Intelligence Layer Objective: Apply org filters to products, requirements, PDF pipeline, and all three intelligence services. Fix productId trim bug and patch the unscoped query at standards-intelligence/service.ts line 201. Expected Outcome: All intelligence layer queries are org-scoped. productId empty-string bug is fixed. Line 201 in standards-intelligence/service.ts is patched.

Task ID: T-3.4.1 Task Title: Apply org filter to products service and handlers Description: Add org filter to all query sites in products/service.ts and all product API handlers. Deliverable: Products service and handlers are org-scoped. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Product queries return only requesting-org products.

Task ID: T-3.4.2 Task Title: Apply org filter to requirements service and handlers Description: Add org filter to all query sites in requirements/service.ts and all requirements API handlers. Deliverable: Requirements service and handlers are org-scoped. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Requirements queries return only requesting-org requirements.

Task ID: T-3.4.3 Task Title: Apply org filter to all 3 PDF pipeline handlers Description: Add org filter to all 3 PDF pipeline handlers: pdf/stats/GET.ts, pdf/source-documents/GET.ts, pdf/source-documents/[id]/GET.ts. The bracket-path handler must use #db/client.js and #db/schema.js import aliases. Deliverable: All 3 PDF pipeline handlers are org-scoped. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: PDF stats and source-document queries return only requesting-org data. Bracket-path handler confirmed to use #db/client.js and #db/schema.js aliases.

Task ID: T-3.4.4 Task Title: Fix productId trim bug and add org stamp in pdf/upload/POST.ts Description: Fix the productId handling to use productId?.trim() || null replacing any empty-string handling. Add organization_id stamp from getIdentity(res).organizationId to the INSERT. Deliverable: pdf/upload/POST.ts correctly handles productId and stamps org ID. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Upload with empty productId stores NULL not empty string. Uploaded document is stamped with requesting org ID.

Task ID: T-3.4.5 Task Title: Apply org filter to all three intelligence services Description: Add org filter to all query sites in product-intelligence/service.ts, deliverable-intelligence/service.ts, and standards-intelligence/service.ts. The unscoped query at standards-intelligence/service.ts line 201 must be patched. Deliverable: All three intelligence services are org-scoped. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Intelligence queries return only requesting-org data. Line 201 in standards-intelligence/service.ts confirmed patched.

Task ID: T-3.4.6 Task Title: Apply org filter to product-knowledge-objects/GET.ts Description: Add org filter to product-knowledge-objects/GET.ts. Deliverable: product-knowledge-objects/GET.ts is org-scoped. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: GET /api/product-knowledge-objects with Org B JWT returns only Org B data.

Sprint Code: WP3-S5 Sprint Name: INSERT Stamps Objective: Audit all 14 INSERT sites, stamp any missing with organization_id from getIdentity(res), and verify zero NULL rows after test inserts. Expected Outcome: All 14 INSERT sites stamp organization_id. Zero NULL rows across all 20 tables after test inserts.

Task ID: T-3.5.1 Task Title: Audit all 14 INSERT sites for org stamp Description: Review all 14 INSERT sites across the codebase. For each site, confirm whether organization_id is already stamped from getIdentity(res).organizationId. Produce a list of sites that are missing the stamp. Run grep -rn "INSERT INTO|.insert(" src/server/ to locate all INSERT sites. Deliverable: Audit report listing all 14 INSERT sites and their stamp status. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: All 14 INSERT sites identified. Each site classified as stamped or missing stamp.

Task ID: T-3.5.2 Task Title: Add org stamp to all missing INSERT sites Description: For each INSERT site identified as missing in T-3.5.1, add organization_id: getIdentity(res).organizationId to the INSERT payload. Deliverable: All INSERT sites stamp organization_id from getIdentity(res). Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-3.5.1 Verification Criteria: All 14 INSERT sites confirmed to include org stamp. No INSERT site missing the stamp.

Task ID: T-3.5.3 Task Title: Verify zero NULL rows after test inserts across all 20 tables Description: Perform a test insert via each of the 14 INSERT paths using an authenticated request. After all test inserts, run SELECT COUNT(*) FROM table WHERE organization_id IS NULL for all 20 tables. All counts must be zero. Deliverable: Verification output showing COUNT = 0 for all 20 tables after test inserts. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-3.5.2 Verification Criteria: All 20 COUNT queries return 0. Output is recorded.

Sprint Code: WP3-S6 Sprint Name: Singleton Replacement Objective: Replace the global singletons in interpretation-config.ts and interpretation-run-store.ts with org-scoped Maps. Convert isGptAvailable to async. Update all callers. Expected Outcome: Both singletons are replaced. isGptAvailable is async. All 6 callers pass organizationId. TypeScript compilation passes. This sprint gates WP4-S1.

Task ID: T-3.6.1 Task Title: Replace interpretation-config.ts singleton with org-scoped Map Description: Replace the global singleton in interpretation-config.ts with a Map<string, KnowledgeAcquisitionConfig> keyed by organizationId. All read and write operations must accept organizationId as a parameter. Convert isGptAvailable() to async isGptAvailable(organizationId: string) — this is required for the resolveAiConfig integration in WP4-S1. Deliverable: interpretation-config.ts uses org-scoped Map. isGptAvailable is async. Estimated Effort: 0.75 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5 Verification Criteria: Setting config for Org A does not affect Org B config. isGptAvailable function signature is async (organizationId: string): Promise<boolean>. TypeScript compilation passes.

Task ID: T-3.6.2 Task Title: Update all 4 callers of interpretation-config Description: Update all 4 call sites that read or write interpretation-config to pass organizationId as a parameter. Locate call sites with grep -rn "interpretation-config" src/. Deliverable: All 4 callers pass organizationId to interpretation-config functions. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-3.6.1 Verification Criteria: TypeScript compilation passes. No call site uses the old singleton API.

Task ID: T-3.6.3 Task Title: Replace interpretation-run-store.ts singleton with org-scoped Map Description: Replace the global singleton in interpretation-run-store.ts with a Map<string, InterpretationRunRecord[]> keyed by organizationId. All read and write operations must accept organizationId as a parameter. Deliverable: interpretation-run-store.ts uses org-scoped Map. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5 Verification Criteria: Run records for Org A are not visible to Org B. TypeScript compilation passes.

Task ID: T-3.6.4 Task Title: Update both callers of interpretation-run-store Description: Update both call sites that read or write interpretation-run-store to pass organizationId. Locate call sites with grep -rn "interpretation-run-store" src/. Deliverable: Both callers pass organizationId to interpretation-run-store functions. Estimated Effort: 0.1 pd Required Capability: Modulance Backend Engineering Dependencies: T-3.6.3 Verification Criteria: TypeScript compilation passes. No call site uses the old singleton API.

Work Package Code: WP-4 Work Package Name: AI Configuration Isolation Objective: Implement org-scoped AI config resolution. Thread organizationId through all four AI call chains. Replace all global key references with resolveAiConfig(organizationId). Convert all 6 isGptAvailable call sites to async. Expected Outcome: No AI call uses a global or fallback key. Every AI chain receives organizationId. resolveAiConfig throws a hard error on missing config. All 6 isGptAvailable call sites use await.

Sprint Code: WP4-S1 Sprint Name: resolveAiConfig Function Objective: Implement resolveAiConfig with org-scoped caching, hard error on missing config, and integration into isGptAvailable. Convert all 6 isGptAvailable call sites to async. Expected Outcome: resolveAiConfig is live with 2-min TTL cache. Hard error confirmed for unknown org ID. isGptAvailable is async and all 6 call sites use await.

Task ID: T-4.1.1 Task Title: Implement resolveAiConfig with org-scoped cache Description: Create src/server/ai/resolve-ai-config.ts. Must maintain a Map<string, { config: AiConfig; expiresAt: number }> cache keyed by organizationId with a 2-minute TTL. On cache miss or expiry, must call GET /api/orgs/:organizationId/ai-config on xxx.ai using a service token. Must throw a typed hard error if the endpoint returns 404 — no fallback to getSecret('OPENAI_API_KEY') or any global key. Deliverable: src/server/ai/resolve-ai-config.ts — org-scoped AI config resolver with 2-min TTL cache and hard error on missing config. Estimated Effort: 1 pd Required Capability: Modulance Backend Engineering Dependencies: T-3.6.2, T-1.1.3, T-1.2.4 Verification Criteria: resolveAiConfig(orgAId) returns Org A config. resolveAiConfig(orgBId) returns Org B config. resolveAiConfig('unknown-org-id') throws typed error. Second call within 2 minutes does not call xxx.ai. Call after 2 minutes calls xxx.ai again.

Task ID: T-4.1.2 Task Title: Confirm hard error on missing config — no global key fallback Description: Confirm that resolveAiConfig throws a typed error — not a silent fallback — when xxx.ai returns 404 for an org ID. The error must be catchable by callers and must not result in any AI call proceeding with a global or fallback key. Deliverable: resolveAiConfig throws typed error on missing config. No fallback to global key exists anywhere in the function. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-4.1.1, T-1.2.4 Verification Criteria: Call with unknown org ID throws typed error. Call with Org B ID does not throw. No getSecret('OPENAI_API_KEY') call exists in resolve-ai-config.ts.

Task ID: T-4.1.3 Task Title: Wire resolveAiConfig into isGptAvailable Description: Update isGptAvailable in interpretation-config.ts to call resolveAiConfig(organizationId) internally. Function must remain async as established in T-3.6.1. Must return true if resolveAiConfig returns a config, false if it throws a hard error. Deliverable: isGptAvailable(organizationId) calls resolveAiConfig and returns correct boolean. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-4.1.2 Verification Criteria: await isGptAvailable(orgAId) returns true. await isGptAvailable('unknown-org-id') returns false without throwing.

Task ID: T-4.1.4 Task Title: Convert all 6 isGptAvailable call sites to async Description: Locate all 6 call sites using grep -rn "isGptAvailable" src/. Update each call site to await isGptAvailable(organizationId). A missed call site silently returns a stale synchronous value — no compile error, no runtime exception, wrong behavior. All 6 must be updated. Deliverable: All 6 isGptAvailable call sites use await isGptAvailable(organizationId). Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-4.1.3 Verification Criteria: grep -rn "isGptAvailable" src/ returns exactly 6 call sites. All 6 use await. TypeScript compilation passes. No synchronous call to isGptAvailable remains.

Sprint Code: WP4-S2 Sprint Name: AI Call Chain Threading Objective: Thread organizationId through all four AI call chains and replace all global key references with resolveAiConfig(organizationId). Expected Outcome: All four AI chains accept and propagate organizationId. All global key references at the 6 specified lines are replaced. TypeScript compilation passes.

Task ID: T-4.2.1 Task Title: Thread organizationId through runGptExtraction Description: Update the runGptExtraction function signature to accept organizationId: string as a parameter. Update all callers to pass organizationId. Deliverable: runGptExtraction accepts and propagates organizationId. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-4.1.4 Verification Criteria: TypeScript compilation passes. runGptExtraction signature includes organizationId parameter. All callers pass the parameter.

Task ID: T-4.2.2 Task Title: Thread organizationId through buildDigitalTwin, interpretLayout, and generateEvidenceAnchors Description: Update the function signatures of buildDigitalTwin, interpretLayout, and generateEvidenceAnchors to accept organizationId: string. Update all callers to pass organizationId. Deliverable: All 3 functions accept and propagate organizationId. Estimated Effort: 0.75 pd Required Capability: Modulance Backend Engineering Dependencies: T-4.1.4 Verification Criteria: TypeScript compilation passes. All 3 function signatures include organizationId. All callers pass the parameter.

Task ID: T-4.2.3 Task Title: Replace global key at lines 738 and 920 of gpt-extraction-engine.ts Description: Replace the hardcoded or global key usage at lines 738 and 920 of gpt-extraction-engine.ts with await resolveAiConfig(organizationId). Deliverable: Lines 738 and 920 use resolveAiConfig(organizationId). Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-4.2.1 Verification Criteria: Lines 738 and 920 confirmed to call resolveAiConfig. No global key reference remains at those lines.

Task ID: T-4.2.4 Task Title: Replace global key at lines 69 and 417 of ai-layout-interpreter.ts Description: Replace the hardcoded or global key usage at lines 69 and 417 of ai-layout-interpreter.ts with await resolveAiConfig(organizationId). Deliverable: Lines 69 and 417 use resolveAiConfig(organizationId). Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-4.2.2 Verification Criteria: Lines 69 and 417 confirmed to call resolveAiConfig. No global key reference remains at those lines.

Task ID: T-4.2.5 Task Title: Replace global key at remaining 4 lines across 3 files Description: Replace global key usage at digital-twin-builder.ts line 114, layout-intelligence/config/PUT.ts line 25, and gpt-diagnostic/GET.ts lines 29 and 32 with await resolveAiConfig(organizationId). Deliverable: All 4 specified lines use resolveAiConfig(organizationId). Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-4.2.2 Verification Criteria: All 4 lines confirmed to call resolveAiConfig. No global key reference remains at those lines. TypeScript compilation passes.

Work Package Code: WP-5 Work Package Name: Admin and Security Hardening Objective: Apply requireRole('admin') guards to all 10 admin route groups. Scope all admin diagnostic queries to the requesting org. Make snapshot export and import org-aware. Export the week4-baseline snapshot. Expected Outcome: All admin routes are role-gated. All admin diagnostic queries are org-scoped. Snapshot export contains only requesting-org data. week4-baseline snapshot is in persistent storage.

Sprint Code: WP5-S1 Sprint Name: Admin Route Guards Objective: Confirm and apply requireRole('admin') to all 10 admin route groups. Verify with member-role JWT. Expected Outcome: All 10 admin route groups return HTTP 403 for member-role token.

Task ID: T-5.1.1 Task Title: Apply requireRole('admin') to all 10 admin route groups Description: Review src/server/entry.ts and confirm that requireRole('admin') middleware is applied to all 10 admin route groups. If any group is missing the guard, add it. The 10 groups are: diag-annotations, diag-canonical, diag-candidates, cleanup-ka-test-data, reset-ka-full, export-snapshot, import-snapshot, seed-products, admin-users, admin-orgs or equivalent group names in the actual codebase. Deliverable: All 10 admin route groups in entry.ts have requireRole('admin') applied. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5 Verification Criteria: All 10 admin route groups confirmed to have requireRole('admin') in entry.ts. No admin route group is missing the guard.

Task ID: T-5.1.2 Task Title: Verify HTTP 403 for member-role token on all 10 admin routes Description: Using a member-role JWT for Test Org B provisioned in T-1.2.3, send authenticated requests to all 10 admin route groups. All must return HTTP 403. Deliverable: Test results confirming HTTP 403 for member-role token on all 10 admin route groups. Estimated Effort: 0.25 pd Required Capability: Modulance QA Dependencies: T-5.1.1, T-1.2.3 Verification Criteria: All 10 admin routes return HTTP 403 when called with Org B member-role JWT. Test results recorded.

Sprint Code: WP5-S2 Sprint Name: Admin Query Scoping Objective: Add org filters to all admin diagnostic and operational query handlers. Restrict seed-products to internal org only. Expected Outcome: All admin queries return only requesting-org data. seed-products is inaccessible to customer orgs.

Task ID: T-5.2.1 Task Title: Apply org filter to admin/diag-annotations/GET.ts Description: Add WHERE organization_id = getIdentity(res).organizationId to the diagnostic annotations query in admin/diag-annotations/GET.ts. Deliverable: admin/diag-annotations/GET.ts returns only requesting-org annotations. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Admin diag-annotations with Org A JWT returns only Org A annotations. With Org B JWT returns only Org B annotations.

Task ID: T-5.2.2 Task Title: Patch line 30 of admin/diag-canonical/GET.ts Description: Add org filter to the canonicalKoAssignments full scan at line 30 of admin/diag-canonical/GET.ts. Deliverable: Line 30 query includes org filter. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Line 30 confirmed patched. Query returns only requesting-org canonical KO assignments.

Task ID: T-5.2.3 Task Title: Patch line 37 of admin/diag-candidates/GET.ts Description: Add org filter to the evidenceAnchors query at line 37 of admin/diag-candidates/GET.ts. Deliverable: Line 37 query includes org filter. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Line 37 confirmed patched. Query returns only requesting-org evidence anchors.

Task ID: T-5.2.4 Task Title: Add org scope validation to cleanup endpoint Description: Add validation to confirm that any scope parameters such as project IDs or document IDs passed to the cleanup endpoint belong to the requesting org before executing the cleanup. Deliverable: Cleanup endpoint validates scope parameters against requesting org. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Cleanup request with Org B JWT and Org A scope parameters returns HTTP 403 or HTTP 400. Cleanup request with Org A JWT and Org A scope parameters succeeds.

Task ID: T-5.2.5 Task Title: Scope full KA reset to requesting org Description: Add org filter to the full KA reset so that it deletes only rows belonging to the requesting org. Must not delete rows belonging to other orgs. Deliverable: Full KA reset affects only requesting-org rows. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Reset with Org A JWT deletes only Org A KA data. Org B data is unaffected.

Task ID: T-5.2.6 Task Title: Scope export-snapshot to requesting org Description: Add WHERE organization_id = getIdentity(res).organizationId to all SELECT * queries in the export-snapshot handler. The exported snapshot must contain only the requesting org data. Deliverable: Exported snapshot contains only requesting-org data. Estimated Effort: 0.5 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5, T-2.3.4 Verification Criteria: Snapshot exported with Org A JWT contains only Org A rows. No Org B rows appear in the Org A snapshot.

Task ID: T-5.2.7 Task Title: Restrict seed-products to internal org admin only Description: Add requireRole('admin') guard and an additional check that the requesting org is the internal org (Org A) to ko/dev/seed-products/POST.ts. This endpoint must not be accessible to customer orgs. Deliverable: seed-products/POST.ts is gated to admin role and internal org only. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5 Verification Criteria: Request with Org A admin JWT succeeds. Request with Org B admin JWT returns HTTP 403. Request with Org A member JWT returns HTTP 403.

Sprint Code: WP5-S3 Sprint Name: Snapshot Org-Awareness Objective: Confirm import-snapshot stamps org ID on all INSERTs and validates org ID match. Export the week4-baseline snapshot for use in Week 4 validation. Expected Outcome: Import handler is fully org-aware. week4-baseline snapshot is in persistent storage and ready for two-org validation.

Task ID: T-5.3.1 Task Title: Confirm all INSERTs in import-snapshot handler stamp org ID Description: Review import-snapshot/POST.ts to confirm that every INSERT executed during snapshot import includes organization_id stamped from getIdentity(res).organizationId. If any INSERT is missing the stamp, patch it. Deliverable: All INSERT operations in import-snapshot/POST.ts stamp organization_id. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.3.5 Verification Criteria: All INSERT statements in the import handler confirmed to include org stamp. Test import produces rows with correct organization_id.

Task ID: T-5.3.2 Task Title: Confirm import-snapshot validates org ID match Description: Confirm that import-snapshot/POST.ts validates that the snapshot embedded org ID matches the requesting org ID before executing any INSERT. If this check is missing, add it. Deliverable: Import handler rejects snapshots whose org ID does not match the requesting org. Estimated Effort: 0.25 pd Required Capability: Modulance Backend Engineering Dependencies: T-5.3.1 Verification Criteria: Import of Org A snapshot with Org A JWT succeeds. Import of Org A snapshot with Org B JWT returns HTTP 403 or HTTP 400.

Task ID: T-5.3.3 Task Title: Export week4-baseline snapshot Description: After all WP-3 query filters are in place and T-5.2.6 is complete, export a full org-scoped snapshot of the Org A dataset. Tag the snapshot week4-baseline. This snapshot is used as the starting state for Week 4 two-org validation. Deliverable: Snapshot tagged week4-baseline saved to persistent storage. Estimated Effort: 0.1 pd Required Capability: Modulance Operations Dependencies: T-5.3.2, T-5.2.6, T-3.1.1, T-3.1.2, T-3.1.3, T-3.1.4, T-3.1.5, T-3.2.1, T-3.2.2, T-3.2.3, T-3.2.4, T-3.3.1, T-3.3.2, T-3.3.3, T-3.3.4, T-3.3.5, T-3.4.1, T-3.4.2, T-3.4.3, T-3.4.4, T-3.4.5, T-3.4.6, T-3.5.1, T-3.5.2, T-3.5.3 Verification Criteria: Snapshot file exists in persistent storage. Snapshot can be restored to a clean DB. Restored DB contains only Org A data. Zero NULL organization_id rows.

Work Package Code: WP-6 Work Package Name: Isolation Validation Objective: Verify all STOP criteria, execute transitive isolation tests, and run the full internal two-org validation suite. All 8 exit criteria must pass before customer access is authorized. Expected Outcome: All 14 STOP criteria are green. All 14 T-EX transitive tests pass. All 59 two-org validation assertions pass. All 8 exit criteria are confirmed. Written sign-off is issued.

Sprint Code: WP6-S1 Sprint Name: STOP Criteria Verification Objective: Verify all 14 STOP criteria across three verification passes covering schema, middleware, backfill, admin guards, query filters, INSERT stamps, singletons, and AI config. Expected Outcome: All 14 STOP criteria confirmed green. Output recorded for each criterion.

Task ID: T-6.1.1 Task Title: Verify STOP criteria S-1 through S-5 Description: Verify STOP criteria S-1 through S-5. S-1: organization_id column exists on all 20 tables. S-2: both Org A and Org B JWTs are accepted by jwtAuthMiddleware. S-3: zero NULL organization_id rows across all 20 tables. S-4: week1-post-backfill snapshot exists in persistent storage. S-5: requireRole('admin') is applied to all 10 admin route groups. Deliverable: Verification output for S-1 through S-5. All 5 criteria confirmed green. Estimated Effort: 0.25 pd Required Capability: Modulance QA Dependencies: T-2.2.4, T-2.3.4, T-2.1.5, T-1.2.5, T-5.1.1 Verification Criteria: All 5 STOP criteria confirmed. Output recorded. No criterion is amber or red.

Task ID: T-6.1.2 Task Title: Verify STOP criteria S-6 through S-9 Description: Verify STOP criteria S-6 through S-9. S-6: all approximately 30 query filter sites patched with spot-check of 10 representative endpoints. S-7: all 14 INSERT sites stamp org ID. S-8: zero NULL organization_id rows after test inserts. S-9: interpretation-config and interpretation-run-store singletons replaced with org-scoped Maps. Deliverable: Verification output for S-6 through S-9. All 4 criteria confirmed green. Estimated Effort: 0.5 pd Required Capability: Modulance QA Dependencies: T-3.1.1, T-3.1.2, T-3.1.3, T-3.1.4, T-3.1.5, T-3.2.1, T-3.2.2, T-3.2.3, T-3.2.4, T-3.3.1, T-3.3.2, T-3.3.3, T-3.3.4, T-3.3.5, T-3.4.1, T-3.4.2, T-3.4.3, T-3.4.4, T-3.4.5, T-3.4.6, T-3.5.1, T-3.5.2, T-3.5.3, T-3.6.1, T-3.6.2, T-3.6.3, T-3.6.4 Verification Criteria: All 4 STOP criteria confirmed. Spot-check of 10 endpoints shows org-scoped responses. Output recorded.

Task ID: T-6.1.3 Task Title: Verify STOP criteria S-10 through S-14 Description: Verify STOP criteria S-10 through S-14. S-10: resolveAiConfig throws hard error for unknown org ID. S-11: all 6 isGptAvailable call sites use await. S-12: all AI engine files use resolveAiConfig not global key. S-13: admin diagnostic queries are org-scoped. S-14: T-EX transitive isolation tests all pass. Deliverable: Verification output for S-10 through S-14. All 5 criteria confirmed green. Estimated Effort: 0.5 pd Required Capability: Modulance QA Dependencies: T-4.1.1, T-4.1.2, T-4.1.3, T-4.1.4, T-4.2.1, T-4.2.2, T-4.2.3, T-4.2.4, T-4.2.5, T-5.2.1, T-5.2.2, T-5.2.3, T-6.2.3, T-1.2.4 Verification Criteria: All 5 STOP criteria confirmed. Output recorded. T-EX test suite results attached.

Sprint Code: WP6-S2 Sprint Name: Transitive Isolation Tests Objective: Execute all 14 T-EX transitive isolation tests. Fix any failures and re-run to confirm all 14 pass. Expected Outcome: All 14 T-EX tests pass. No child or join record is accessible cross-org.

Task ID: T-6.2.1 Task Title: Execute T-EX-1 through T-EX-14 transitive isolation tests Description: Execute all 14 transitive isolation tests. Each test must: create a parent record as Org A, create a child or join record linked to the parent as Org A, attempt to access the child or join record using an Org B JWT, and confirm the child or join record is not returned. Tests must cover all child and join tables that are transitively isolated through their parent organization_id. Deliverable: T-EX-1 through T-EX-14 test run results. Estimated Effort: 2 pd Required Capability: Modulance QA Dependencies: T-3.1.1, T-3.1.2, T-3.1.3, T-3.1.4, T-3.1.5, T-3.2.1, T-3.2.2, T-3.2.3, T-3.2.4, T-3.3.1, T-3.3.2, T-3.3.3, T-3.3.4, T-3.3.5, T-3.4.1, T-3.4.2, T-3.4.3, T-3.4.4, T-3.4.5, T-3.4.6, T-3.5.1, T-3.5.2, T-3.5.3, T-1.2.3 Verification Criteria: All 14 tests pass. No child or join record is accessible cross-org. Test results recorded.

Task ID: T-6.2.2 Task Title: Fix failing T-EX tests (conditional) Description: For each failing T-EX test, identify the unfiltered query or missing org stamp, apply the fix, and re-run the affected test. This task is conditional and only executes if T-6.2.1 produces failures. Deliverable: All previously failing T-EX tests now pass. Estimated Effort: Variable Required Capability: Modulance Backend Engineering Dependencies: T-6.2.1 Verification Criteria: All previously failing tests now pass. Fix is merged to main.

Task ID: T-6.2.3 Task Title: Re-run full T-EX suite after fixes (conditional) Description: After all fixes from T-6.2.2 are applied, re-run the full T-EX-1 through T-EX-14 suite to confirm all 14 tests pass. This task is conditional and only executes if T-6.2.2 was executed. Deliverable: Full T-EX re-run results showing all 14 tests passing. Estimated Effort: 0.5 pd Required Capability: Modulance QA Dependencies: T-6.2.2 Verification Criteria: All 14 T-EX tests pass. No failures. Results recorded.

Sprint Code: WP6-S3 Sprint Name: Internal Two-Org Validation Objective: Restore week4-baseline, execute all 59 two-org validation assertions across 6 assertion groups, fix any failures, and issue written sign-off confirming all 8 exit criteria pass. Expected Outcome: All 59 assertions pass. All 8 exit criteria are confirmed green. Written sign-off is issued. Customer org provisioning in WP1-S3 is unblocked.

Task ID: T-6.3.1 Task Title: Restore week4-baseline and verify starting state Description: Restore the week4-baseline snapshot to the preview database. Verify that Org A data is present and intact. Verify that Org B has no data rows across all 20 tables. This is the starting state for all Week 4 validation tests. Deliverable: Preview DB restored to week4-baseline. Org A data confirmed present. Org B data confirmed absent. Estimated Effort: 0.5 pd Required Capability: Modulance Operations Dependencies: T-5.3.3, T-1.2.5 Verification Criteria: SELECT COUNT() FROM projects WHERE organization_id = OrgAId returns greater than 0. SELECT COUNT() FROM projects WHERE organization_id = OrgBId returns 0. Same check on all 20 tables.

Task ID: T-6.3.2 Task Title: Execute V-VIS-1 through V-VIS-10 cross-org visibility assertions Description: Execute 10 cross-org visibility assertions. Each assertion confirms Org A can see its own data on a given endpoint and confirms Org B cannot see Org A data on the same endpoint. Cover the 10 highest-risk endpoints: projects, source-documents, annotations, canonical-ko, digital-twins, evidence-anchors, KOs, products, requirements, KPIs. Deliverable: V-VIS-1 through V-VIS-10 test results. Estimated Effort: 1 pd Required Capability: Modulance QA Dependencies: T-6.3.1, T-1.2.5 Verification Criteria: All 10 assertions pass. Org B receives empty arrays or HTTP 404 for all Org A resources. Results recorded.

Task ID: T-6.3.3 Task Title: Execute V-MUT-1 through V-MUT-9 cross-org mutation assertions Description: Execute 9 cross-org mutation assertions. Each assertion attempts to mutate an Org A resource using an Org B JWT. All 9 must return HTTP 404 or HTTP 403. Assertions must cover: annotation update, annotation delete, annotation promote at line 181, source-document archive, source-document delete, canonical-ko merge, digital-twin reprocess, KO update, project update. Deliverable: V-MUT-1 through V-MUT-9 test results. Estimated Effort: 1 pd Required Capability: Modulance QA Dependencies: T-6.3.1, T-3.1.6, T-1.2.5 Verification Criteria: All 9 mutation attempts return HTTP 404 or HTTP 403. No Org A resource is mutated by an Org B request. Results recorded.

Task ID: T-6.3.4 Task Title: Execute V-AI-1 through V-AI-7 AI config isolation assertions Description: Execute 7 AI config isolation assertions. V-AI-1: Org A GPT extraction uses Org A key. V-AI-2: Org B GPT extraction uses Org B key. V-AI-3: Org A key is not used for Org B extraction. V-AI-4: resolveAiConfig(OrgAId) returns Org A config. V-AI-5: resolveAiConfig(OrgBId) returns Org B config. V-AI-6: isGptAvailable(OrgBId) returns correct boolean. V-AI-7: no global key fallback occurs for either org. Deliverable: V-AI-1 through V-AI-7 test results. Estimated Effort: 1 pd Required Capability: Modulance QA Dependencies: T-6.3.1, T-4.2.1, T-4.2.2, T-4.2.3, T-4.2.4, T-4.2.5, T-1.2.4 Verification Criteria: All 7 assertions pass. No AI call uses the wrong org key. Results recorded.

Task ID: T-6.3.5 Task Title: Execute V-INTERP-1 through V-INTERP-5 interpretation isolation assertions Description: Execute 5 interpretation isolation assertions. V-INTERP-1: setting interpretation config for Org A does not affect Org B. V-INTERP-2: Org A run history is not visible to Org B. V-INTERP-3: Org B run history is not visible to Org A. V-INTERP-4: isGptAvailable returns correct value per org. V-INTERP-5: interpretation run store is keyed by org ID. Deliverable: V-INTERP-1 through V-INTERP-5 test results. Estimated Effort: 0.5 pd Required Capability: Modulance QA Dependencies: T-6.3.1, T-3.6.1, T-3.6.2, T-3.6.3, T-3.6.4 Verification Criteria: All 5 assertions pass. Interpretation state is isolated per org. Results recorded.

Task ID: T-6.3.6 Task Title: Execute V-ADMIN-1 through V-ADMIN-6 admin route isolation assertions Description: Execute 6 admin route isolation assertions. V-ADMIN-1: Org B member-role token returns HTTP 403 on all admin routes. V-ADMIN-2: Org A admin diag-annotations returns only Org A data. V-ADMIN-3: Org A admin diag-canonical returns only Org A data. V-ADMIN-4: Org A admin diag-candidates returns only Org A data. V-ADMIN-5: seed-products is inaccessible to Org B admin. V-ADMIN-6: export-snapshot with Org A JWT contains no Org B rows. Deliverable: V-ADMIN-1 through V-ADMIN-6 test results. Estimated Effort: 0.5 pd Required Capability: Modulance QA Dependencies: T-6.3.1, T-5.1.1, T-5.1.2, T-5.2.1, T-5.2.2, T-5.2.3, T-5.2.4, T-5.2.5, T-5.2.6, T-5.2.7, T-1.2.3 Verification Criteria: All 6 assertions pass. Results recorded.

Task ID: T-6.3.7 Task Title: Execute V-SNAP-1 through V-SNAP-5 snapshot isolation assertions Description: Execute 5 snapshot isolation assertions. V-SNAP-1: Org A snapshot export contains only Org A rows. V-SNAP-2: Org B snapshot export contains only Org B rows. V-SNAP-3: import of Org A snapshot with Org B JWT is rejected. V-SNAP-4: import of Org A snapshot with Org A JWT succeeds and stamps Org A ID on all rows. V-SNAP-5: after Org A snapshot import, Org B data is unaffected. Deliverable: V-SNAP-1 through V-SNAP-5 test results. Estimated Effort: 0.5 pd Required Capability: Modulance QA Dependencies: T-6.3.1, T-5.3.1, T-5.3.2, T-5.3.3, T-1.2.5 Verification Criteria: All 5 assertions pass. Results recorded.

Task ID: T-6.3.8 Task Title: Execute V-FLOW-1 through V-FLOW-12 end-to-end flow assertions and issue sign-off Description: Execute 12 end-to-end flow assertions simulating a new customer org performing a complete workflow as Org B. V-FLOW-1: Org B authenticates. V-FLOW-2: Org B creates a project. V-FLOW-3: Org B uploads a PDF. V-FLOW-4: Org B triggers GPT extraction. V-FLOW-5: Org B views extracted annotations. V-FLOW-6: Org B promotes an annotation. V-FLOW-7: Org B creates a canonical KO. V-FLOW-8: Org B views KPIs. V-FLOW-9: Org B exports a snapshot. V-FLOW-10: Org B data is not visible to Org A. V-FLOW-11: Org B AI calls use Org B key. V-FLOW-12: Org B snapshot contains only Org B data. All 12 must pass. This is the final gate before customer access is authorized. Deliverable: V-FLOW-1 through V-FLOW-12 test results. All 8 exit criteria confirmed. Written sign-off recorded. Estimated Effort: 1 pd Required Capability: Modulance QA Dependencies: T-6.3.2, T-6.3.3, T-6.3.4, T-6.3.5, T-6.3.6, T-6.3.7, T-1.2.5 Verification Criteria: All 12 V-FLOW assertions pass. All 8 exit criteria EC-1 through EC-8 confirmed green. Sign-off document produced. T-1.3.1 customer org provisioning is unblocked.

Task ID: T-6.3.9 Task Title: Fix failing exit criteria and re-run affected assertion groups (conditional) Description: For each exit criterion that fails in T-6.3.2 through T-6.3.8, identify the root cause, apply the fix, and re-run the affected test group. All 8 exit criteria must pass before T-6.3.8 sign-off is issued. This task is conditional and only executes if failures are found. Deliverable: All previously failing exit criteria now pass. Fixes merged to main. Affected test groups re-run. Estimated Effort: Variable Required Capability: Modulance Backend Engineering and Modulance QA Dependencies: T-6.3.2, T-6.3.3, T-6.3.4, T-6.3.5, T-6.3.6, T-6.3.7, T-6.3.8 Verification Criteria: All 8 exit criteria confirmed green. No exit criterion is amber or red. Re-run results recorded.

Work Package Code: WP-7 Work Package Name: Customer Onboarding Objective: Provision the real customer org, verify JWT acceptance and AI config resolution, implement audit events, perform snapshot cycle validation, and grant customer access. Expected Outcome: Customer is authenticated, has created their first project, triggered GPT extraction using their own AI key, and has been granted access. Audit events are emitted for all 5 required event types.

Sprint Code: WP7-S1 Sprint Name: Customer Org Provisioning Objective: Confirm customer org ID is received from xxx.ai and verify resolveAiConfig returns the customer's own AI config. Expected Outcome: Customer org ID is recorded by Modulance. resolveAiConfig(customerOrgId) returns customer-specific AI config.

Task ID: T-7.1.1 Task Title: Record customer org ID from xxx.ai Description: After T-1.3.1 completes, confirm that the customer org ID issued by xxx.ai is communicated to the Modulance team. This ID is required for any Modulance-side provisioning steps and for verifying resolveAiConfig(customerOrgId). Deliverable: Customer org ID confirmed and recorded by Modulance team. Estimated Effort: 0.1 pd Required Capability: Modulance Operations Dependencies: T-1.3.1 Verification Criteria: Customer org ID is recorded. Modulance team has the value.

Task ID: T-7.1.2 Task Title: Verify resolveAiConfig returns customer AI config Description: Confirm that T-1.3.2 is complete. Verify that resolveAiConfig(customerOrgId) returns the customer's own AI config from the Modulance side. Deliverable: resolveAiConfig(customerOrgId) returns customer AI config. Estimated Effort: 0.1 pd Required Capability: Modulance Operations Dependencies: T-7.1.1, T-1.3.2, T-4.1.1 Verification Criteria: resolveAiConfig(customerOrgId) returns non-empty config. Config key matches customer's own key.

Sprint Code: WP7-S2 Sprint Name: Customer JWT Verification Objective: Execute all 6 JWT verification tests using real customer credentials. Expected Outcome: All 6 T-JWT tests pass. Customer JWT is accepted, claims are correct, and admin route access is correctly enforced.

Task ID: T-7.2.1 Task Title: Execute T-JWT-1 through T-JWT-6 with real customer credentials Description: Execute the 6 JWT verification tests using the real customer JWT. T-JWT-1: customer JWT is accepted by jwtAuthMiddleware. T-JWT-2: getIdentity(res).organizationId returns customer org ID. T-JWT-3: getIdentity(res).role returns admin. T-JWT-4: customer JWT is rejected after expiry. T-JWT-5: customer can access a protected API route. T-JWT-6: customer cannot access admin routes if customer org is not the internal org where applicable. Deliverable: T-JWT-1 through T-JWT-6 test results with real customer credentials. Estimated Effort: 1 pd Required Capability: Modulance QA Dependencies: T-7.1.2, T-1.3.3 Verification Criteria: All 6 tests pass. Results recorded.

Sprint Code: WP7-S3 Sprint Name: Audit Events and Snapshot Cycle Objective: Implement the 5 required audit event types. Perform a snapshot export and restore cycle with the customer org present. Expected Outcome: All 5 audit event types are emitted with org attribution. Snapshot cycle confirms customer data isolation. Org A data is unaffected.

Task ID: T-7.3.1 Task Title: Implement 5 required audit event types Description: Implement audit event emission for the 5 required event types: user login, project created, document uploaded, annotation promoted, snapshot exported. Each event must include organizationId, userId, eventType, timestamp, and relevant resource ID. Events must be attributable to the requesting org via getIdentity(res).organizationId. Deliverable: 5 audit event types emitted on the corresponding actions. Events include org attribution. Estimated Effort: 1 pd Required Capability: Modulance Backend Engineering Dependencies: T-2.1.5 Verification Criteria: Each of the 5 actions produces an audit event. Event payload includes organizationId matching the requesting org. Events are queryable.

Task ID: T-7.3.2 Task Title: Perform snapshot export and restore cycle with customer org Description: With the customer org provisioned and at least one project created, perform a snapshot export and restore cycle. Verify that the snapshot contains only customer org data, the restore succeeds, and Org A data is unaffected. Deliverable: Snapshot and restore cycle completed with customer org present. Org A data confirmed unaffected. Estimated Effort: 0.5 pd Required Capability: Modulance QA Dependencies: T-7.2.1, T-5.3.2, T-1.3.3 Verification Criteria: Customer snapshot contains only customer org rows. Restore succeeds. Org A row counts are unchanged after restore.

Sprint Code: WP7-S4 Sprint Name: Customer Access Grant Objective: Deliver customer credentials, guide first login, and confirm end-to-end workflow including GPT extraction with customer AI key. Expected Outcome: Customer has logged in, created a first project, and triggered GPT extraction using their own AI key. No Org A data is visible to the customer.

Task ID: T-7.4.1 Task Title: Deliver credentials and confirm customer first workflow Description: Deliver the customer login credentials including email and initial password or login link. Guide the customer through their first login. Verify that the customer can create a project, upload a document, and trigger GPT extraction using their own AI key. Confirm that resolveAiConfig(customerOrgId) returns the customer key during the extraction. Deliverable: Customer credentials delivered. Customer has logged in. First project created. GPT extraction triggered successfully. Estimated Effort: 0.5 pd Required Capability: Modulance Operations Dependencies: T-7.2.1, T-7.3.2, T-1.3.4 Verification Criteria: Customer login succeeds. First project exists in DB with customer organization_id. GPT extraction completes using customer AI key. No Org A data is visible to the customer.*

Completed
in 4m 55s
